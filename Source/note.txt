처음부터는 못했지만 지금부터라도 필기 시작.

GameFeature라는 시스템
GameStateComponent 는 GameState에 붙힐 수 있는 부품
확장성 좋음 / 디버깅 힘들다
ExperienceManager를 GameState에 붙힐거다.

InitGameState에서 로딩이 되었는지 확인
ExperienceManager에서 로딩 되었는지 체크

Login
InitNewPlayer -> UpdatePlayerStartSpot -> FindPlayerStart_Implementation -> ChoosePlayerStartImplementation
-> GetDefaultPawnClassForController_Implementation -> GetPawnDataForController
LoginEnd

PostLogin
HandleStartingNewPlayer_Implementation -> RestartPlayerAtPlayerStart

이 때 아직 ExperienceComponenet가 붙어있지 않으니 플레이어 정보를 저장해놨다가 나중에 불러들임

CDO -> 기본값들을 UObect 방식으로 만들어 놓은 것. 기본값의 모음. 기본값을 저장하는 UObject
시리얼라이제이션 하는 과정에서 델타 시리얼라이제이션/직렬화를 지원하기 위함
델타시리얼라이제이션 : 기본값이랑 비교해서 다른 데이터를 저장 --> 메모리 절약(언리얼은 데디케이트서버 기반이기 때문에 서버에 바뀐 부분만 전달하면 됨)

-카메라
카메라 컴포넌트에서 OnRegister에서 스택 생성.
GetCameraVeiw가 매 프레임 호출 -> UpdateCameraMode ->DetermineCameraModeDelegate를 통해 모드를 받아오고 스택에 저장
HeroComponent의 HandleChangeInitState 단계에서  DetermineCameraModeDelegate 바인딩 DefaultCameraMode를 반환하는


네트워크 시리얼라이즈 방식을 사용하기 위해 FDlCharacterPartList / FDlAppliedCharacterPartEntry 로 나눈거임